# V2.2 production QA checklist

Run on **production** after PR 5 merges: https://matrix-themed-sprint-planner.vercel.app

**Prerequisites:** All migrations through `20250520120000_backfill_general_project.sql` applied to the **same** Supabase project as Vercel env vars.

**Ship steps (GitHub, Vercel, screenshots):** [v2.2-ship-guide.md](./v2.2-ship-guide.md)

Record: date, tester, pass/fail, notes.

---

## Visual / theme (PR 4)

- [ ] Ink + matrix-green text on planner (not flat white-on-black only).
- [ ] Mono uppercase labels on form fields and section headings.
- [ ] Magenta primary buttons (Sign in, Add task, Create).
- [ ] Red **Register** CTA on `/register`; red Register link on login footer.
- [ ] Subtle green grid / texture visible on backgrounds.
- [ ] Tab focus ring visible on interactive elements.

---

## Empty states & errors (PR 5)

| Page | Empty state | Error (trigger if safe) |
|------|-------------|-------------------------|
| `/` planner | Clear message when no tasks / no sprint / no project | Workspace load error shows `InlineAlert` |
| `/done` | Clear when no done tasks; archive toggle explained | Restore/delete failure is readable |
| `/invites` | No pending invites shows steps | RLS error is friendly (not raw policy only) |
| `/login` | n/a | Invalid credentials styled |
| `/register` | n/a | Validation/auth errors styled |

- [ ] Member sees helpful text when waiting for admin to create sprint/project.
- [ ] Admin-only actions show friendly message if forced (or UI disabled).

---

## Projects (PR 3)

- [ ] **Admin:** create, rename, delete project (cannot delete last project or one with sprints).
- [ ] Project switcher filters sprints and tasks.
- [ ] New sprint is tied to active project.
- [ ] New task inherits project from sprint (`tasks.project_id` in Supabase).
- [ ] **Member:** can switch projects; cannot manage projects.

---

## Task tracker (PR 2)

- [ ] Statuses: todo → in progress → blocked → done.
- [ ] Blocked can store a reason.
- [ ] Marking **done** removes task from planner; appears on `/done`.
- [ ] **Archive completed** on Done hides tasks; **Show archived** reveals them.
- [ ] **Restore** returns task to planner as todo.
- [ ] Optional due date on create works.

---

## About (PR 1)

- [ ] `/about` loads when logged out.
- [ ] GitHub / LinkedIn links work.
- [ ] Links from login and planner footer reach `/about`.

---

## Collaboration (two accounts — recommended)

**A** = admin, **B** = member. **A** invites **B**’s email; **B** registers with that email.

- [ ] Shared display names sync via `workspace_profiles`.
- [ ] Assignee on task persists after refresh for both users.
- [ ] **B** cannot manage projects/sprints/invites (UI + friendly error if applicable).
- [ ] **B** can CRUD tasks.

---

## Routes & deploy

- [ ] `/login`, `/register`, `/invites`, `/done`, `/about` — no Vercel 404.
- [ ] Hard refresh on `/` while signed in stays on planner.

---

## Docs & proof

- [ ] README lists V2.2 features and migration #6–#7.
- [ ] Screenshots in `docs/images/` (or README states “coming soon” until you add PNGs).
- [ ] `docs/v2.2.md` progress table: PRs 1–5 marked merged.

---

## Sign-off

| Section | Pass | Tester | Date |
|---------|------|--------|------|
| Visual / theme | [ ] | | |
| Empty states & errors | [ ] | | |
| Projects | [ ] | | |
| Task tracker | [ ] | | |
| About | [ ] | | |
| Collaboration | [ ] | | |
| Routes / deploy | [ ] | | |
| Docs / proof | [ ] | | |

**V2.2 complete when all required sections pass.**
