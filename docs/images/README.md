# Screenshots for README

Add PNG captures of **production** (or post-merge preview) here:

| File | Page |
|------|------|
| `login.png` | `/login` |
| `planner.png` | `/` (planner) |
| `done.png` | `/done` |
| `invites.png` | `/invites` |
| `about.png` | `/about` (optional) |

See **[v2.2-ship-guide.md](../v2.2-ship-guide.md)** Phase 3 for capture steps.

After adding files, commit to `main`:

```bash
git add docs/images/*.png
git commit -m "docs: add V2.2 production screenshots"
git push origin main
```
